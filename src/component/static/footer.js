import React from 'react';

function Footer(props) {
  return (<div id={props.id} className={props.className}>
      <p>References: Redux, React Router</p>        
    </div>);
}

export default Footer;